name: Deploy to cPanel via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    # Tên job đã được đổi lại cho ngắn gọn, nhưng không cần thiết
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📤 Upload files to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5 
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          
          # Thử nghiệm đường dẫn tương đối (từ thư mục gốc FTP của bạn)
          server-dir: anhdung.viakingvn.com 
          
          local-dir: ./
          
          # SỬ DỤNG GIAO THỨC FTPS (An toàn và thông dụng hơn)
          protocol: ftps 
          
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/README.md
