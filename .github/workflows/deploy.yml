name: Deploy to cPanel via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📤 Upload files to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5 
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          
          # Đã sửa lỗi đường dẫn: Dùng đường dẫn tuyệt đối chính xác từ CPanel
          server-dir: /home/viakingv/anhdung.viakingvn.com/ 
          
          local-dir: ./
          
          # Giữ lại FTPS để bảo mật. Thử FTP nếu vẫn lỗi.
          protocol: ftps 
          
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/README.md
